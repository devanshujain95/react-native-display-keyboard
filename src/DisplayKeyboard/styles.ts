import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  view: {
    flexDirection: 'row',
    flexWrap: 'wrap',
  },
  cellStyle: {
    justifyContent: 'center',
    alignItems: 'center',
    flexBasis: '33.33%',
    borderBottomWidth: 1,
    borderRightWidth: 1,
  },
});
